import styles from '../styles/contact.module.css'

const contact = () => {
  return (
      <>
    <h1 className= {styles.cntus}>Hello form the contact side</h1>

    {/* As you can see 3 classed for component base class we have to do little hard word
    and below is the method both above too */}
    {/* <button className={`${styles.btn} ${btn-primary} ${mx-2}`}>SUbmit</button> */}
    </>
  )
}

export default contact